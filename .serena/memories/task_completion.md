# Task Completion Checklist

1. No formal linters/formatters — bash scripts are not linted
2. No test suite — scripts are tested by running them
3. After changes, verify with `bash -n scripts/<file>.sh` for syntax check
4. Commit with descriptive messages
5. Push to `main` branch
